{
  "name": "ssl-node-test",
  "type": "module",
  "scripts": {
    "start": "npm test && npm run stats",
    "test": "node server-tests",
    "stats": "which python ",
    "ghp": "bundle exec jekyll serve --incremental"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
